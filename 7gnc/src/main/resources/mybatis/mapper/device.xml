<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gnc.dao.DeviceDao">
    <insert id="registerDao">
      insert into TBL_device(a,b,c,d)
      values
      (#{param1},#{param2},#{param3},#{param4})
    </insert>
       
        <select id="deviceListDao" resultType="com.gnc.dto.DeviceDto" parameterType="string"> 
select a,b,c,d from TBL_device order by a LIMIT ${pageStart}, ${perPageNum}</select>
	
	
	<select id="getCount" resultType="int">
    SELECT count(*) FROM TBL_device
</select>

<update id="updateDao">
    update TBL_device set a = #{a}, b = #{b}, c = #{c},
     d = #{d} where a = #{a}
    </update>
      
    <select id="deviceDao" resultType="com.gnc.dto.DeviceDto">
		select a,b,c,d from TBL_device where a = #{a}
	</select>
	
		<select id="searchDao" resultType="com.gnc.dto.DeviceDto">
		select a,b,c,d from TBL_device where a LIKE CONCAT('%',#{keyword},'%') LIMIT ${pageStart}, ${perPageNum}
	</select>
	
	
<select id="getSearchCount" resultType="int">
    SELECT count(*) FROM TBL_device where a LIKE CONCAT('%',#{keyword},'%')
</select>
	
	<delete id="deleteDao" >
		 delete from TBL_device where a = #{a}
	</delete>
	
</mapper>